@model List<Product>
<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Product Name</th>
            <th scope="col">Category</th>
            <th scope="col">Price</th>
        </tr>
    </thead>
    <tbody>
        @{
            foreach (var product in Model)
            {
                <tr>
                    <th scope="row">#</th>
                    <td>@product.Name</td>
                    <td>@product.Category</td>
                    <td>@product.Price</td>
                    <td>
                        <a asp-controller="Product" asp-action="Edit" asp-route-id="@product.Id"> <i class="bi bi-pencil-square me-2"></i> 
                        </a>
                    </td>
                </tr>
            }
        }
    </tbody>
</table>
